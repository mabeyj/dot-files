# Personal preferences for shell commands

## Arguments

When writing documentation or scripts, I prefer using long-form arguments
instead of shorthand for shell commands, such as `--force` instead of `-f`,
since they are self-documenting.

Feel free to use shorthand everywhere else.
